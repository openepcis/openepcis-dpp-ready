<?xml version="1.0" encoding="UTF-8"?>
<!-- GENERATED by scripts/build-gitb-testsuite.ts — do not edit. -->
<testcase id="tc-selftest-eu.battery.model" xmlns="http://www.gitb.com/tdl/v1/"
          xmlns:gitb="http://www.gitb.com/core/v1/">
  <metadata>
    <gitb:name>[eu.battery.model] Self-test: the shapes accept correct and reject broken passports</gitb:name>
    <gitb:version>0.9.9</gitb:version>
    <gitb:description>Runs without interaction. Validates the reference passports OpenEPCIS publishes for EU Battery — model granularity (EN 18223), which must conform, and a deliberately broken variant, which must NOT. Without the second half a green result would be indistinguishable from empty shapes. Fixtures are pre-expanded N-Quads so the outcome depends on the shapes alone and not on the currently deployed contexts.</gitb:description>
  </metadata>
  <actors>
    <gitb:actor id="DPPDataProvider" name="Digital Product Passport data provider" role="SUT"/>
  </actors>
  <steps>
    <verify id="pos0" handler="http://shacl-validator:8080/shacl/soap/dpp/validation?wsdl"
            desc="Reference passport must conform: amperia-staxwall-model.jsonld">
      <input name="contentToValidate">$pos0_amperia_staxwall_model</input>
      <input name="validationType">"eu.battery.model"</input>
      <input name="embeddingMethod">"STRING"</input>
      <input name="contentSyntax">"application/n-quads"</input>
    </verify>
    <verify id="pos1" handler="http://shacl-validator:8080/shacl/soap/dpp/validation?wsdl"
            desc="Reference passport must conform: battery-product-model.jsonld">
      <input name="contentToValidate">$pos1_battery_product_model</input>
      <input name="validationType">"eu.battery.model"</input>
      <input name="embeddingMethod">"STRING"</input>
      <input name="contentSyntax">"application/n-quads"</input>
    </verify>
    <!-- The passport's GS1 Digital Link says one granularity and oec:granularityLevel claims another, which dpp-sh:GranularityDigitalLinkConstraint must reject (EN 18219 4.4 / EN 18223). This is a SHACL-SPARQL constraint, so it also proves the Test Bed's engine evaluates sh:sparql at all. -->
    <verify id="neg" handler="http://shacl-validator:8080/shacl/soap/dpp/validation?wsdl" invert="true"
            desc="Broken passport must be REJECTED (granularity-mismatch)">
      <input name="contentToValidate">$neg_granularity_mismatch</input>
      <input name="validationType">"eu.battery.model"</input>
      <input name="embeddingMethod">"STRING"</input>
      <input name="contentSyntax">"application/n-quads"</input>
    </verify>
  </steps>
  <output>
    <success>
      <default>"The eu.battery.model shapes accept every reference passport and reject the broken one."</default>
    </success>
    <failure>
      <default>"Self-test failed: either a reference passport was rejected or the broken passport was accepted. Both mean the eu.battery.model shapes are not fit to certify anyone."</default>
    </failure>
  </output>
</testcase>

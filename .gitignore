# Dependencies
node_modules/
.pnpm-store/
.yarn/
.npm/

# Build outputs
dist/
build/
.turbo/
.output/
.cache/
*.tsbuildinfo

# Editor / OS
.DS_Store
# Office lock/owner files (created while an .xlsx/.docx is open)
~$*
.idea/
.vscode/
*.swp
*.swo

# Env
.env
.env.*
!.env.example

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Claude Code — allow shared config but not machine-local state
.claude/settings.local.json

# Playwright MCP debug output
.playwright-mcp/

# Generated product images (managed via scripts/upload-product-images.sh → S3/MinIO)
scripts/images/

# vocab-sync transient run outputs (the sync report regenerates each run; mappings land on a branch)
docs/skos-completeness-sync.json
docs/skos-completeness-sync.md
docs/skos-completeness-sync-32b.json
docs/skos-completeness-sync-32b.md
# curation review workbook (edited locally; accepted mappings land on a branch)
docs/skos-alignment-review.xlsx
